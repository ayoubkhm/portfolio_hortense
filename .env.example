DATABASE_URL="file:./dev.db"
RESEND_API_KEY="re_your_api_key_here"
FROM_EMAIL="onboarding@resend.dev"
NOTIFICATION_EMAIL="your@email.com"
SITE_URL="https://hortensederuidiaz.fr"

# Google Maps Platform (Place Details API for Google Reviews sync).
# CID = decimal Google Business Profile identifier. Get it from a Maps URL like
# https://maps.google.com/?cid=<DECIMAL>.
GOOGLE_MAPS_API_KEY="AIza..."
GOOGLE_PLACE_CID="6877207829907698706"

# Cron secret protecting /api/cron/* routes. Generate via `openssl rand -hex 32`.
CRON_SECRET="generate_with_openssl_rand_hex_32"
